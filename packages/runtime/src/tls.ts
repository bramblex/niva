
const tls = globalThis.Niva.tls;
export const TLSSocket = tls.TLSSocket;
export const Server = tls.Server;
export const connect = tls.connect;
export const createServer = tls.createServer;
export default tls;
