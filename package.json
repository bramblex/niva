{
  "private": true,
  "workspaces": [
    "packages/devtools",
    "packages/types"
  ],
  "devDependencies": {
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "type-checker": "cd ./packages/devtools && npx tsc --noEmit && cd ../.."
  }
}
