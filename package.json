{
  "private": true,
  "workspaces": [
    "packages/devtools",
    "packages/types",
    "packages/runtime"
  ],
  "devDependencies": {
    "husky": "^8.0.0",
    "@types/node": "22.14.0"
  },
  "scripts": {
    "prepare": "husky install",
    "type-checker": "npm run build --workspace=packages/runtime && npm run typecheck --workspace=packages/types && npm exec --workspace=packages/devtools -- tsc --noEmit"
  }
}
